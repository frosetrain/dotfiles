- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    # Terminal
    ~/.zshrc: zshrc
    ~/.config/nvim: nvim

    # Developer tools
    ~/.gitconfig: gitconfig
    ~/.config/VSCodium/User/settings.json: vscode.json

